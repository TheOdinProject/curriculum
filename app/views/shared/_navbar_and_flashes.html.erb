<%= render 'shared/navbar' %>

<div class="nav-spacer"></div>

<%= render 'shared/admin_flash' unless @is_home_page %>

<%= render "layouts/#{flash[:partial]}" if flash[:partial] %>

<%= render 'shared/flashes', flash: flash unless flash.empty? %>
