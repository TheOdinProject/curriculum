<div class="settings-section">
  <h2 class="accent settings-section__heading">Learning Goal</h2>
  <div class="settings-card card-main">

    <div class="settings-card__edit-button settings-card__edit-button--open"></div>

    <div class="settings-view">
      <p class="form__label">Learning goal</p>
      <p class="form__element--non-editable learning_goal"><%= display_learning_goal(@user) %></p>
    </div>

    <%= form_for @user, html: { class: "form settings-view--hidden" }, remote: true do |form| %>

    <div class="form__section">
      <%= form.label :learning_goal, class: 'form__label' %>
      <%= form.text_field :learning_goal, class: 'form__element', maxlength: 100, size: 140 %>
    </div>

      <%= form.submit "Update", class: "button button--primary full-width" %>
    <% end %>
  </div>
</div>
