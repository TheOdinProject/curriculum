$('.lesson-button-group').html("<%= j render 'lessons/lesson_buttons', lesson: @lesson, course: @lesson.course, user: @user  %>");
$('.banner__badge').html("<%= j render 'shared/course_badge', course: @lesson.course, user: @user %>");
